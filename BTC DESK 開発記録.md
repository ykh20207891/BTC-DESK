# BTC DESK 開発記録

Bybit の BTCUSDT 無期限先物を対象にした自動売買デスク。6つのデスク（SPOTTER→PRIOR→EDGE→KELLY→TAKER→CLOSER）が1枚のチケットを順に回し、すべての判断をログに残す。

- リポジトリ: https://github.com/ykh20207891/BTC-DESK
- 起動: `python run.py` → http://127.0.0.1:8765
- 関連ノート（同じフォルダ）: [[README]]（起動・運用手順）、[[PRODUCT]]（製品の前提）、[[DESIGN]]（デザインシステム）

---

## 現状（2026-09-15 時点）

- **モード**: PAPER（Bybit メインネットの実データ、約定はシミュレーション。手数料 0.055%・スリッページ込み）
- **元本**: $500（`.env` の `PAPER_SEED_EQUITY=500`）。$10,000 → $100 → $500 と変えてきた。古い台帳は `data/backup/` に退避
- **戦略**: 15分足 UP/DOWN。RSI・EMA・VWAP乖離・zバンド・出来高・k-NNアナログで P(UP) を推定 → 板の偏りから市場の織り込みを推定 → エッジ 1¢ 以上ならハーフケリーでサイズ → 成行 → ホライズン（次の15分足）か損切り/利確で決済
- **リスク上限（初期値）**: レバ 3×、1チケット 2%、日次DDガード 4.2%、損切り 0.6%、利確 0.9%、同時保有 5
- **検証**: `python tools/check.py` が ALL GREEN（lint + 25テスト + ライブスモーク）

## 決定事項

| 日付 | 決定 | 理由 |
|---|---|---|
| 09-10 | スタックは Python(FastAPI) + 素の HTML/JS/Canvas | 24時間動く単一プロセス、中央のアニメーションを Canvas で自作 |
| 09-10 | まずメインネットのペーパートレード | 実データで注文フローを検証、資金リスクなし |
| 09-10 | 他社ブランド（GROK DESK・マスコット）は使わず BTC DESK と独自マーク | 参考画像は見た目の参照のみ |
| 09-15 | 中央フロアを「6ステーション＋コア」のサイバーHUD風に差し替え | 2枚目の参考画像に忠実に |
| 09-15 | PC とスマホでレイアウトを分離（スマホは下部タブ型） | PCはそのまま、iPhone では1画面ずつ |
| 09-15 | 元本 $500 | $100 だと最小数量 0.001 BTC(≈$76) が Kelly 上限を超えてほぼ見送りになる |

## 構成

```
backend/   engine.py（6デスクの司令塔）, model.py（確率・エッジ・ケリー）, analog.py（k-NN）,
           bybit.py（REST/WS クライアント）, broker.py（paper/live）, storage.py（SQLite）, server.py（API/WS）
frontend/  index.html, app.js, floor.js（中央フロア）, charts.js, i18n.js（EN/JA）, app.css, fonts/
tools/check.py   検証ループ（lint + テスト + --live スモーク + --watch）
tests/           合成取引所（FakeREST/FakeWS）で6デスクを一周する 25 テスト
data/            ledger.sqlite（台帳）, settings.json, engine.log
```

## 運用メモ

- 常時稼働: `powershell -ExecutionPolicy Bypass -File run_forever.ps1`（落ちたら自動再起動）
- 稼働確認: `GET /api/health`（ストリーム切断・足の停止で 503）
- 日次DDガードと日中ピークは再起動をまたいで保持される（再起動でガードは解除されない）
- LIVE 切替: `.env` に API キー → 画面の PAPER チップ → エンジン再起動。LAN 公開時は `DESK_TOKEN` 必須
- 台帳のリセット（元本変更）: エンジン停止 → `data/ledger.sqlite*` を退避 → `.env` の `PAPER_SEED_EQUITY` を変更 → 起動

## 変更履歴

### 2026-09-10
- 初回構築（エンジン、6デスク、ダッシュボード、README）。GitHub へ初回プッシュ
- デザインレビュー2周（fix→ship）。DESIGN.md 記録
- 日本語/英語切替（ログ行もキー＋パラメータで翻訳）
- ユーザー指摘で反映: 観葉植物を削除、中央への点線を調整、床のカーペットを楕円に

### 2026-09-15
- 中央フロアを新しい参考画像に合わせて全面差し替え（発光リング、プラットフォーム、ビーム、各ステーションのカード、四隅HUD）
- 文字サイズを全体的に約1.25倍（キャンバスは1.3倍）
- カクつき対策: shadowBlur 全廃、背景を裏画面にキャッシュ、30fps 上限
- 左側3枚のカードはステーションの左側に配置
- iPhone 用レイアウト（下部タブ FLOOR / DESKS / BOOK / TAPE、縦向きフロア）
- 実運用の問題洗い出し → 修正（下記）と検証ループ環境（tools/check.py、GitHub Actions）
- 元本 $100 → $500

### 09-15 に修正した実運用上の問題
- 起動直後の最初の15分足確定を取りこぼしていた
- 切断中に閉じた足の見逃し → 再接続時バックフィル、足自体から確定を検出、実行中の確定は待ち行列へ
- 再起動でドローダウンガードが解除されていた → 永続化
- 承認待ちチケットが残り続ける／ルーティング途中で固まる → 期限切れ・放棄を自動処理
- 注文数量の丸め誤差、最小数量・最小注文額・上限数量の未考慮
- ライブ: 約定を取引所から読み戻す、決済は取引所の実ポジション量、未約定は取り消し、ヘッジモード拒否、起動時に突合
- REST リトライと時刻同期、ストリーム無音の監視、設定値の範囲検証、操作APIのトークン認証、`/api/health` の 503 化、ローテーションログ、DB 肥大化抑制

## 未決・次の候補

- **学習機能**: 決済済みチケットの特徴量→勝敗でモデル重みをオンライン更新する層。まず影モード（提案のみ）で比較し、十分なサンプル後に切替。リスク上限は学習で緩めない
- **外部からのアクセス**: Cloudflare Tunnel（改修なし、推奨）／ Supabase + Vercel + VPS（ログイン・履歴クラウド化、半日〜1日の改修）。エンジンはどちらでも PC か VPS に置く必要がある
- **通知**（Telegram/LINE）、**VPS 移設**
- 戦略の成績は本稼働後に評価（ペーパーの結果はまだ数十件）
